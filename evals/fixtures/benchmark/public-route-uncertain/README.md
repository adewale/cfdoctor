# Public category pages

`categories.example` has public `/category/:slug` pages over a 50,000-row D1 table. The repository contains neither an `EXPLAIN QUERY PLAN` result nor measured rows-read evidence, and supplied configuration contains no cache evidence. No deployed traffic or account controls are supplied.
