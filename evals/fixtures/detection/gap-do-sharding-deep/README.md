# Deep Durable Object sharding indirection

The singleton key is composed in one module and imported by the Worker. The fixture verifies bounded static constant/alias resolution without claiming arbitrary data-flow analysis.
