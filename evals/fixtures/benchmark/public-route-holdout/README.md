# Public timeline

`chronicle.example` is preparing a public issue timeline over a growing 80,000-entry corpus. The homepage footer links the timeline, while the sitemap contains only the homepage. There is no cache configuration, traffic history, query plan, rows-read measurement, or account export.
