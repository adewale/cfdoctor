# dead-rpc-surface

Worker fixture with a Durable Object that exposes non-runtime public methods. The scanner should not prove they are dead, but it should create a lead to run an optional cross-boundary RPC reachability review such as deadlint.
