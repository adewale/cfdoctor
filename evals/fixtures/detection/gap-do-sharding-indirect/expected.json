{
  "description": "Worker that routes all traffic through one Durable Object, but the singleton key is held in a named constant (idFromName(COORDINATOR_KEY)) instead of a string literal, so the literal-only hotspot heuristic misses it.",
  "evidence_ids": [
    "CFDOC-EVD-COEY-DO-GOTCHAS"
  ],
  "required_check_ids": [
    "DO-SHARDING-HOTSPOT"
  ],
  "forbidden_check_ids": []
}
