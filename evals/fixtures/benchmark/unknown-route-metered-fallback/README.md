# Public company handbook

The public company handbook is served from `handbook.example.com`. Navigation
and topic indexes link to about 3,500 nested handbook entries, while the sitemap
contains only `/` and `/topics`. Content editors can publish nested paths without
a Worker deployment. No request logs, D1 Query Insights, cache configuration,
WAF rules, bot controls, rate limits, or billing exports are supplied.
