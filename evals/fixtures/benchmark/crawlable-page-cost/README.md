# Public research catalogue

`catalogue.example` is about to launch with 35,000 abstracts. The generated sitemap lists only `/` and `/browse`; the shared navigation links `/about`, and rendered browse results link `/abstract/:id` pages. The Worker has no cache configuration. No deployed traffic, WAF, bot, rate-limit, billing, or account evidence is supplied.
